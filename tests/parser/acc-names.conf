tackler {
  core {
    basedir = ok/

    input.fs.dir = "not-used/"
    input.fs.glob = "**.txn"

    reporting.reports = [ ]
    reporting.exports = [ "identity" ]

    include "acc-names-coa.conf"
  }
}
